delta = self.hp * 0.5 + nearest.hp * 0.5;
target = "";
command = "";
if(isAdjacent == true){
target = "furthest.name";
command = "attack";
return action(command, target);
}
  else{
command = "move";
target = "furthest.name";
return action(command, target );
}
